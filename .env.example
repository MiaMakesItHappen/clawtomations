# Optional: override default OpenClaw launch mode and command.
# CLI command template support variables: {{workflow}}, {{output}}, {{site}}, {{env.VAR}}
CLAWTOMATIONS_OPENCLAW_COMMAND="openclaw run --workflow {{workflow}} --output {{output}}"
CLAWTOMATIONS_OPENCLAW_BIN="openclaw"

# Optional: where this repo should be copied in notifications
CLAWTOMATIONS_OPENCLAW_NOTIFY_URL="http://127.0.0.1:8010"
